{
  "name": "@cli-me/api",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "migrate": "node dist/scripts/migrate.js",
    "seed": "node dist/scripts/seed.js",
    "sync:seed": "node dist/scripts/sync-seed.js",
    "refresh:checksums": "node dist/scripts/refresh-checksums.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cli-me/shared-types": "0.1.0",
    "@fastify/cors": "^10.0.2",
    "@types/pg": "^8.16.0",
    "fastify": "^5.2.1",
    "nanoid": "^5.1.5",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "pg-mem": "^3.0.11",
    "tsx": "^4.20.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.5"
  }
}
